# deprecated/

- `HANDOFF.md` — original tool-build handoff (Harry Mayne's playground →
  tinkerscope: discovery replacing `models.yaml`, samplescope plumbing to graft
  in, inference plumbing). Deprecated 2026-06-22: the build it describes is done
  and its file/line refs predate the `src/tinkerscope/` restructure (and the
  drop of latteries for the direct tinker SDK). Its durable content was folded
  into `../CLAUDE.md` (external reference paths, box facts, doc map) and lives
  more accurately in code docstrings (`src/tinkerscope/api/discovery.py`,
  `tinker_sampler.py`). Kept for history — the original design rationale.
